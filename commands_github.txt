#initialize a git repo
#do once per project
      git init


# after you code, at a good stopping point 
# this will add to git
      git add . 

#this labels your progress, your code versions
     git commit -m "what you did"

#can do one of two THINGS
#do once per project
#you will keep coding and then repeat adding and committing

#If you are ready to push to github
go into github, and create a new repo (upper right corner, +, new repository)
we will copy the SSH url, make sure it's on the SSH not HTTPS
#do once per project
    git remote add origin *paste the SSH URL here

#push to github
    git push origin master


#start

git init
git add . 
git commit -m 'message'
git remote origin *paste SSH URL
git push origin master


#continue coding and continue pushing to github
git add . 
git commit - m 'message'
git push origin master